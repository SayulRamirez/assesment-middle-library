server:
  port: 8082

spring:
  application:
    name: loan-microservice
  data:
    mongodb:
      uri: ${URI_MONGO_DB}
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always